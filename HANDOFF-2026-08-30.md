# HANDOFF 2026-08-30 — DCTV-OPD 主实验 4-seed launch on new cluster

## TL;DR

- **新集群 `$CLUSTER_PREFIX`**（旧集群 `ts-69aDaB...` 已失效）。可用节点 8 台：launcher + worker-0..6（README 里 worker-7..14 hostname 解析不了，实际未分配）。
- **DCTV-OPD 4-seed 主实验已起飞** 在 worker-0..3 上 2026-08-30 03:33 起，用 4 台 (~30 min 后 04:03 全部到 step 4/2000)。
- **剩下 4 台 (launcher + worker-4..6) 目前 idle** — 只有 occupy.py。用户决定"后面再考虑剩下的机器用来干啥"。
- **代码：DCTV method 落地完成**，5 文件修改，已通过语法 + 功能 smoke test。
- **Teacher server 未变**：`<REDACTED_IP>:15555` (JustRL-DeepSeek-1.5B, ZMQ, tp=2, 4 workers)。

## 4 runs 一览

| Node | Method | Seed | wandb 名 | 远端 log |
|------|--------|------|----------|---------|
| worker-0 | dctv | 42 | `OPD_DS_DS_DAPO_adv_dctv_seed42` | `/tmp/ds_ds_dapo_dctv_seed42.log` |
| worker-1 | dctv | 63 | `OPD_DS_DS_DAPO_adv_dctv_seed63` | `/tmp/ds_ds_dapo_dctv_seed63.log` |
| worker-2 | dctv | 7  | `OPD_DS_DS_DAPO_adv_dctv_seed7`  | `/tmp/ds_ds_dapo_dctv_seed7.log`  |
| worker-3 | dctv | 21 | `OPD_DS_DS_DAPO_adv_dctv_seed21` | `/tmp/ds_ds_dapo_dctv_seed21.log` |

Seeds 42/63/7/21 与既有 sign/raw 的 4-seed 完全对齐 → method × seed heatmap 可以 1:1 对齐。

## DCTV 方法（DCTV_OPD_method.md）
```
A_i,t   = c_t · sign(Δ_i,t)                          ← 每 token 只保留 pure-TV 方向
c_0      = 1                                          ← bootstrap 第一步等价 pure sign
c_{t+1}  = min(c_cap, D̄_t / (M̄_t + ε))               ← past-conditioned scalar
D̂_t     = mean_i [1 - exp(Δ_i)]_+                    ← TV distance estimator
M̂_t     = 0.5 · η_t · (G_t^TV)^2                     ← predicted 1-step TV motion
G_t^TV   = G_t^scaled / max(c_t, ε)                   ← pre-clip grad_norm 逆 c 缩放
D̄, M̄ = β·EMA(β_D=β_M=0.95)   c_cap=1.0   ε=1e-8      ← 全部估计量 smoothing，不是 method hparam
```
Warmup 已删（初始化 c_0=1 天然 bootstrap，不需要）。真正的 method hparam 现在剩 c_cap（默认 1.0，主实验就是 1.0）。

## 代码修改（Aug 29-30 session）
- `verl/workers/config/actor.py` — PolicyLossConfig 加 `opd_dctv_beta_d/beta_m/eps/c_cap`（4 个）。
- `verl/trainer/config/actor/actor.yaml` + `_generated_ppo_(megatron_)trainer.yaml` — 同步。
- `verl/trainer/ppo/core_algos.py`
  - `_apply_adv_transform` 加 `dctv_c` kwarg + `dctv` mode：`signs * mask * dctv_c`。
  - `compute_policy_loss_opd` 加 `dctv_c_current` kwarg；在 pre-transform Δ diagnostics 之后计算 D̂（`[1-exp(Δ)]_+` on eff_mask），emit sum+count metrics；transform 传 c；新 metric：`opd_dctv_D_sum/D_count/D_hat_microbatch/c_current` + 4 个 hparam echo。
- `verl/workers/actor/dp_actor.py`
  - `__init__` 初始化 `_dctv_state = {c:1.0, D_ema:None, M_ema:None, step:0}`。
  - `update_policy` 顶部：dctv 与 `use_kl_loss=True` 或 `entropy_coeff≠0` 互斥（raise ValueError；防 KL/entropy 污染 grad_norm 破坏 M̂）。
  - 每 mini-batch 局部累加 `_dctv_D_sum_local` + `_dctv_D_count_local`；loss 调用只在 `loss_mode=="opd"` 时传 `dctv_c_current`。
  - 每次 `_optimizer_step()` 之后：`all_reduce(sum,count)` → `D_hat`；`G_tv = G_scaled/max(c,ε)`；`M_hat = 0.5·lr·G_tv²`；EMA 首值 copy；`c_next = min(c_cap, D_ema/(M_ema+ε))`；non-finite grad_norm 时冻结 controller + M_hat/G_tv 记 NaN + `nonfinite_grad_norm=1` flag。
- 完整 DCTV metric 集（16 keys，per optimizer step）：
  - Raw：`D_hat / D_count / G_scaled / G_tv / M_hat`
  - EMA：`D_ema / M_ema`
  - Controller：`R / c_used / c_next`
  - Runtime：`lr / step / nonfinite_grad_norm`
  - Per microbatch：`D_sum / D_count / D_hat_microbatch / c_current`
  - Hparam echoes：`beta_d / beta_m / eps / c_cap`

## 新脚本

- `train_script/ds_ds_dapo_dctv_exp.sh` — Per-run trainer。与 phase2_exp 同 hparam；`opd_adv_mode=dctv` + 4 个 DCTV knob 从 env 读入（默认全为方法 doc 值）。硬编码 `use_kl_loss=False entropy_coeff=0`，dp_actor 也会 double-check。
- `train_script/launch_ds_ds_dapo_dctv.sh` — 4-seed dispatcher（`worker-0..3`, seeds 42/63/7/21）。用 `ssh -f -n` 派发（避 wait 卡死）。
- `train_script/prewarm_ds_distill_qwen15b.sh` — CLUSTER_PREFIX 已更新为新集群，节点列表截到 8 台。
- `CLUSTER_QUICKSTART.md` — 集群重启后到训练起飞的完整 8 步 runbook（含 occupy.py 说明、prewarm、netrc、prefix 迁移、监控/kill、常见坑）。

## 集群拓扑变化（对照 08-27）

| 项 | 08-27 | 08-30 |
|---|---|---|
| Cluster prefix | `$CLUSTER_PREFIX` | **`$CLUSTER_PREFIX`** |
| 可用节点 | launcher + worker-0..6 (8 台) | 同（README 写 16 但 worker-7..14 hostname 解析失败） |
| 训练在跑 | 8 (2 phase-2 + 6 replicate) | 4 (dctv 主实验) |
| `/root/DS-Distill-Qwen-1.5B` | 已 prewarmed | ✅ 已 prewarmed 8/8 (`COPIED 3.4G`) |
| Wandb | via `<netrc-path>` | 已重写 `<netrc-path>` (netrc 空 → 写入) |
| Teacher | `<REDACTED_IP>:15555` ✓ | 同，`TEACHER_ALIVE` ✓ |

## 常用监控

```bash
CLUSTER=$CLUSTER_PREFIX
declare -A TAGS=(
  [worker-0]=dctv_seed42
  [worker-1]=dctv_seed63
  [worker-2]=dctv_seed7
  [worker-3]=dctv_seed21
)
for n in "${!TAGS[@]}"; do
  tag=${TAGS[$n]}
  ssh -o ConnectTimeout=4 "${CLUSTER}-${n}" "
    proc=\$(pgrep -f 'verl.trainer.main_ppo' | wc -l)
    err=\$(grep -cE 'CUDA out of memory|zmq.*Timeout|TimeoutError.*teacher|RuntimeError|^Traceback' /tmp/ds_ds_dapo_${tag}.log 2>/dev/null || echo 0)
    tprog=\$(grep -oE 'Training Progress: *[0-9]+%[^\"]*[0-9]+/2000' /tmp/ds_ds_dapo_${tag}.log 2>/dev/null | tail -1)
    gpu=\$(nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader | awk '{s+=\$1}END{printf \"%.0f\", s/NR}')
    printf '${n}/${tag}: proc=%s err=%s gpu=%s%% TP=\"%s\"\n' \$proc \$err \$gpu \"\$tprog\"
  " &
done; wait
```

**判活规则**：`GPU util` 优先于 `log mtime`（Ray log 有 buffering，起步 3-5 min 静默正常）；判死看 `err > 0` **且** GPU util 掉到 0。

## 起飞时间线（本轮）

| 时间 | 事件 |
|---|---|
| ~03:20 | 探集群拓扑 (新 prefix)、8 台探活成功、teacher `TEACHER_ALIVE`、8 台 `/root/DS-Distill-Qwen-1.5B` MISSING |
| ~03:24 | 更新 prewarm 和 dispatcher 里的 CLUSTER_PREFIX；prewarm 完 8/8 `COPIED 3.4G` |
| ~03:30 | 写 `<netrc-path>`（此前空）；dry-run dispatcher 通过 |
| **03:33** | `bash launch_ds_ds_dapo_dctv.sh --go` 4 runs 分派 (seed 42/63/7/21 on worker-0..3) |
| ~03:34 | 4 runs 起 python3 / NCCL init |
| ~03:38 | 4 runs 走完 FSDP wrap，进入 val_before_train |
| ~03:53 | 4 runs `Training Progress: 0/2000, step:0` — 到 training loop 入口 |
| ~03:58 | 3/4 到 `TP: 1/2000`（worker-0 稍慢） |
| **~04:03** | **4/4 全部 `TP: 4/2000`**（3 min per training step，per-optimizer step:275..390 累计） |

## 稳定性验证（04:03 时刻）
- 4 runs 全部 `proc=3`（python3 + 2 ray procs）
- 4 runs 全部 `err=0`（no OOM / no zmq timeout / no traceback）
- GPU util 47-100%（training loop 中，间歇性 vLLM rollout 时略降属正常）
- Training Progress: 4/2000 全部一致 → 判活成功

## 已知坑（沿用 + 本次补）

1. **首次 poll GPU util 显示 0%**：`awk` 除以卡数四舍五入 + polling 时机可能命中 rollout 间隙。**用 nvidia-smi 单卡 raw 值验证**，不看 aggregate mean。
2. **`step:` 与 `Training Progress:` 语义不同**：`step:N` 是 mini-batch/microbatch 级 optimizer step 计数（400 step ≈ 1 training step），`Training Progress: N/2000` 才是外层 training step。
3. **new pod 后 `<netrc-path>` 会空** — wandb key 需重写（quickstart §3）。
4. **新集群 pod 重启会换 hostname prefix** — 至少改 4 个 dispatcher 脚本（quickstart §4）。
5. 沿用 08-27 handoff 的：ssh 并发不串行、GPU util 优先于 log mtime、`_inductor fx cache` W-level 非致命、不要 `pkill 'ray::'` 一把梭（会误杀 teacher）、pod 重启需 prewarm。

## 下一步待办

- [ ] step 25 首次 val：对齐 raw/sign 4-seed baseline 的 aime24/25 acc（DCTV 前中期预测 c≈1 → 曲线应与 sign 几乎重合）
- [ ] step 100+ 稳定性观察
- [ ] wandb 里追踪最重要的一张图（method §7）：eval_acc / D_ema / R=D̄/M̄ / c_next 四条对齐 → 判 R 何时降到 ~1 触发 c<1
- [ ] **剩下 4 台闲置节点** (launcher + worker-4..6) 分配决策：
  - (a) 起 DCTV 更多 seed（101/202/303/404）加深 mean±std
  - (b) 起补齐的 alloc_power / strength_interp 多 seed（08-27 时被 kill 让位给 replicate）
  - (c) 保持 idle 等 DCTV 结果
- [ ] 训完后画 method × seed heatmap（raw/sign/dctv × 4 seeds）on aime24 + aime25

## 参考

- **DCTV 方法论**：`DCTV_OPD_method.md`
- **集群 quickstart runbook**：`CLUSTER_QUICKSTART.md`（本次新增）
- **上一轮 handoff**：`HANDOFF-2026-08-27.md`
- **代码入口**：
  - Transform：`verl/trainer/ppo/core_algos.py:_apply_adv_transform` (dctv 分支 ~L1300)
  - Loss D̂ 估计：`verl/trainer/ppo/core_algos.py:compute_policy_loss_opd`
  - Controller：`verl/workers/actor/dp_actor.py:update_policy` (每 `_optimizer_step` 之后)
- **trainer / dispatcher 脚本**：`train_script/ds_ds_dapo_dctv_exp.sh` + `train_script/launch_ds_ds_dapo_dctv.sh`
